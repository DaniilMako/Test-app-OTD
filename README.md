# 🚀 Задание 5: Реализация собственного сервера с KPI и Alembic
**ВКР по "Основам технического документоведения"**

Расширение React-приложения с добавлением:
- ✅ **Локального FastAPI-сервера**
- ✅ **Базы данных (PostgreSQL)**
- ✅ **Учёт посещений и времени на страницах**
- ✅ **Миграции через Alembic**
- ✅ **Статистика в реальном времени**

---

## 🛠 Как запустить

### 1. Установите зависимости (React)
```bash
cd task6/frontend
npm install
```

### 2. Установите сервер (Python)
```bash
cd ../backend
pip install -r requirements.txt
```

### 3. Настройте БД (.env)
Создайте файл (если его нет) backend/.env:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=otd
DB_USER=postgres
DB_PASS=admin
SECRET_KEY=changeme
```
🔐 Убедитесь, что PostgreSQL запущен и база otd создана.

### 4. Примените миграции
```bash
alembic upgrade head
```
✅ Создаст таблицы pages и kpi

### 4.1. Инициализируйте роли и админа
```bash
cd backend
python init_roles.py
python init_admin.py
```
✅ Создаст роли (user, admin) и админа:
- Email: `admin@example.com`
- Пароль: `admin`

### 5. Запустите сервер (в отдельном терминале)
```bash
uvicorn backend.main:app --reload --port 8000
```
✅ Сервер будет доступен: [http://localhost:8000](http://localhost:8000)

Или второй вариант:
```bash
python -m uvicorn backend.main:app --reload --port 8000
```

### 6. Создайте страницы через Swagger
   1. Перейдите: http://localhost:8000/docs
   2. Используйте POST /admin/create_page 4 раза:
```json
{ "title": "введение" }
{ "title": "описание" }
{ "title": "заключение" }
{ "title": "API" }
```

### 7. Запустите React-приложение
```bash
npm start
```
✅ Приложение будет доступно: [http://localhost:3000](http://localhost:3000)

> 💡 Откройте два терминала: один для сервера, один для React.

---

## 🚢 Запуск через Docker Compose

Хотите поднять PostgreSQL, FastAPI и React одной командой?

1. (Необязательно) создайте в корне `.env`, чтобы переопределить значения по умолчанию:
   ```
   POSTGRES_DB=otd
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=admin
   SECRET_KEY=changeme
   ```
2. Запустите все сервисы:
   ```bash
   docker compose up --build
   ```
3. После старта:
   - Backend API: http://localhost:8000 (Swagger/Redoc на /docs и /redoc)
   - React UI (Nginx): http://localhost:3000
   - PostgreSQL: localhost:5432 (логин/пароль из переменных окружения)

Контейнер backend автоматически выполняет:
- `alembic upgrade head` — применяет миграции
- `init_roles.py` — создаёт роли (user, admin)
- `init_admin.py` — создаёт админа (admin@example.com / admin)

**Учётные данные админа по умолчанию:**
- Email: `admin@example.com`
- Пароль: `admin`

⚠️ **Важно:** Смените пароль админа перед развертыванием в продакшн!

Остановить сервисы — `docker compose down`, для очистки данных удалите volume `postgres_data`.

---

## 📌 Основные возможности

- ✅ **Навигационная панель**: Переключайтесь между вкладками
- ✅ **Сбор KPI**:
   - 📈 Счётчик посещений
   - ⏱️ Время, проведённое на странице
- ✅ **Статистика**: Вкладка /stats — отображает все метрики
- 🔌 **Локальный сервер**: FastAPI с двумя эндпоинтами:
   - `GET /admin/kpis` → статистика
   - `POST /admin/kpi/{id}/time` → обновление времени
   - `POST /admin/users/role` → смена роли пользователя (только для админа)
   - `GET /posts` → посты (проксирует JSONPlaceholder)
   - `POST /invert-image` → инвертирует изображение
- ✅ **Alembic + SQLAlchemy**: Code-first подход к БД
- 🌐 **Запросы к серверу**: React обращается к `http://localhost:8000/posts`
- 🖼️ **Обработка изображений**: Загрузка → инверсия → отображение
- 📚 **Документация API**: /docs, /redoc
- 🔁 **Мемоизация данных**: `useMemo` в компоненте "Посты"
- ✅ **Проксирование API**: GET /posts → JSONPlaceholder
- 🔄 **Сравнение методов**: `axios` vs `fetch` (можно переключать)

---

## 📁 Структура проекта


```
task6/
│
├── backend/
│   ├── main.py
│   ├── database.py
│   ├── config.py
│   ├── models/
│   │   └── models.py           # Таблицы: pages, kpi
│   ├── routers/
│   │   └── admin_router.py     # Эндпоинты: KPI, создание страниц
│   ├── schemas/
│   │   └── schemas.py          # Pydantic-модели
│   ├── migrations/             # Alembic
│   ├── alembic.ini
│   ├── requirements.txt
│   └── .env
│
├── frontend/
│   ├── public/
│   ├── src/
│   │   ├── components/
│   │   │   ├── StatsPage.js    # Отображение статистики
│   │   │   ├── Sidebar.js
│   │   │   └── ...
│   │   ├── App.js              # Логика: таймер, обновление KPI
│   │   └── ...
│   ├── package.json
│   └── ...
│
├── README.md                   # Этот файл
└── .gitignore
```

---

📊 Как работает статистика?
При заходе на вкладку:  
→ GET /admin/pages/{id} → увеличивает visits  
При уходе:
→ Отправляется POST /admin/kpi/{id}/time с количеством секунд  
На вкладке /stats:
→ GET /admin/kpis → отображает все данные

---

## 🌐 Доступ из интернета

**По умолчанию сайт доступен только локально** (`localhost:3000`). 

Для доступа из интернета (чтобы другие люди могли зайти):
- 📖 **См. подробное руководство:** [`DEPLOYMENT.md`](DEPLOYMENT.md)
- 🚀 **Варианты:** VPS, облачные платформы, ngrok, домашний сервер
- 🔒 **Важно:** Настройте HTTPS, файрвол и безопасные пароли

**Быстрый старт для VPS:**
1. Установите Docker на сервер
2. Склонируйте проект
3. Создайте `.env` с `ALLOWED_ORIGINS=http://ваш-домен.com`
4. Запустите `docker compose up -d`

---

## 🔐 CORS

Настроено в `backend/main.py` через переменную окружения `ALLOWED_ORIGINS`:
- По умолчанию: `http://localhost:3000`
- Для внешних доменов: укажите через запятую в `.env`:
  ```env
  ALLOWED_ORIGINS=http://localhost:3000,https://ваш-домен.com
  ```


---

## ✅ Что реализовано?

| Функционал | Реализовано | 
|--------------------------------------------|-------------| 
| Навигационная панель | ✅ | 
| FastAPI-сервер | ✅ | 
| React → сервер | ✅ | 
| Загрузка и инвертирование изображений | ✅ | 
| Документация API (/docs, /redoc) | ✅ | 
| Мемоизация (useMemo) | ✅ | 
| Поддержка fetch | ✅ | 
| База данных (PostgreSQL) | ✅ | 
| Миграции (Alembic) | ✅ | 
| Счётчик посещений | ✅ | 
| Учёт времени на странице | ✅ | 
| Вкладка "Статистика" | ✅ |
---

## 📚 Ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [JSONPlaceholder](https://jsonplaceholder.typicode.com/)
- [React OpenAPI Renderer](https://github.com/tamediadigital/react-openapi-renderer) (альтернатива)
- [Pillow (PIL)](https://pillow.readthedocs.io/) — обработка изображений

---

## 📄 Лицензия

Проект распространяется под лицензией MIT. Подробности в файле `LICENSE`.