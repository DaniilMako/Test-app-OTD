#!/bin/bash
# Скрипт запуска фронтенда на сервере
# Использование: ./start-frontend.sh [API_URL]
# Пример: ./start-frontend.sh http://your-server-ip:8000

cd "$(dirname "$0")/../frontend" || exit 1

# Получение API URL из аргумента или переменной окружения
API_URL=${1:-${REACT_APP_API_URL:-"http://localhost:8000"}}

echo "🔧 Используется API URL: $API_URL"
echo "🚀 Запуск React приложения..."

# Установка переменной окружения и запуск
REACT_APP_API_URL=$API_URL npm start
